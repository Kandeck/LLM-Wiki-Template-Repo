---
type: guide
created: 2026-06-01
updated: 2026-06-01
tags:
  - wiki/guide
---

# 使用说明

这是一个用 Karpathy `llm-wiki` 思路搭建的 Obsidian 自动维护结构。

## 你负责什么

- 把值得长期保存的资料放进 `RAW/`。
- 暂时不确定要不要处理的资料先放进 `INBOX/`。
- 向 Codex 提问题、指出重点、要求健康检查。

## Codex 负责什么

- 读取原始资料，但不改写 `RAW/`。
- 在 `WIKI/` 里创建和更新总结、概念页、实体页、工作流页。
- 每次维护后更新 [[index]]。
- 每次维护后追加 [[log]]。
- 发现矛盾、过时内容、孤立页面和缺失链接。

## 常用命令

- “处理 `RAW/文件名.md`，更新 wiki、index 和 log。”
- “基于这个问题查一下 vault，答案有长期价值就归档。”
- “做一次 wiki 健康检查，能修的直接修，需要我判断的列出来。”
- “把 `INBOX/` 里的资料按主题导入。”


## Importer 插件怎么用

Obsidian 的 **Importer** 是官方维护的社区插件，用来把其他应用或文件格式里的资料迁移成 Obsidian 可读的 Markdown。官方说明见：[Importer - Obsidian Help](https://obsidian.md/help/plugins/importer)。

推荐用法：

1. 在 Obsidian 打开 **设置 → 第三方插件/Community plugins**。
2. 搜索并安装 **Importer**，然后启用。
3. 按插件向导选择来源格式或导出的文件/文件夹。
4. 导入目标建议先选 `INBOX/`，不要直接导入 `WIKI/`。
5. 导入后先快速检查标题、附件、图片、表格和乱码问题。
6. 确认值得长期保存的资料，再移动到 `RAW/`，或让 Codex/Claudian 帮你整理：

```text
检查 INBOX/ 里刚用 Importer 导入的资料，告诉我哪些适合移入 RAW，哪些需要清理。
```

如果资料已经确认要进入知识库，可以说：

```text
把 INBOX/ 里这批 Importer 导入的资料整理到 RAW，并处理 RAW，更新 wiki、index 和 log。
```

注意：Importer 主要解决“迁移/转换格式”的问题，不等于自动维护 wiki。它把资料放进 vault 后，仍然需要 Codex/Claudian 按本说明的导入工作流处理。

## Claudian/Codex 怎么用

你可以把 Claudian/Codex 当作这个 vault 的 wiki 维护员。最常见的交互方式是直接用自然语言发任务：

- `处理 RAW/文件名.md，更新 wiki、index 和 log。`
- `把 INBOX/ 里的资料按主题导入。`
- `基于这个问题查一下 vault，答案有长期价值就归档。`
- `做一次 wiki 健康检查，能修的直接修，需要我判断的列出来。`
- `把 PDF/ 下的新 PDF 转成 Markdown 放到 RAW，然后导入 wiki。`

也可以利用当前笔记、选中文本或文件引用：

- 当前正在看的笔记会作为上下文传给 Claudian/Codex。
- 选中文本后提问，可以让它只处理选中部分。
- 在请求里写 `@文件名.md` 或明确路径，可以让它先读取指定文件。

推荐指令格式是：

```text
动作 + 文件/范围 + 重点 + 是否更新 index 和 log
```

例如：

```text
处理 RAW/xxx.md，重点关注它和英语学习策略的关系，更新 wiki、index 和 log。
```

```text
查询这个 vault：英语学习成功者和不成功者的核心差异是什么？如果答案有长期价值，归档到 WIKI/Questions/。
```

## 这套知识库能做什么

这套结构不是单纯“存文件”，而是把资料逐步编译成一个可持续维护的个人 wiki。它可以做：

- **资料导入**：把文章、PDF 转出来的 Markdown、网页剪藏等整理成 `WIKI/Sources/` 摘要。
- **概念沉淀**：把多篇资料反复出现的主题抽成概念页，例如 `WIKI/Concepts/主题名称`。
- **跨资料综合**：把多篇资料的共同结论、冲突和开放问题整理成综合页，例如 `WIKI/Synthesis/主题综合`。
- **带来源回答问题**：回答问题时优先读 [[index]] 和 `WIKI/`，必要时回到 `RAW/` 核验，并在回答中链接来源。
- **持续维护**：随着新资料进入，更新旧概念页，而不是每次从零开始总结。
- **健康检查**：发现孤立页面、缺失摘要、重复概念、过时判断、互相冲突的说法。
- **把好问题沉淀下来**：有长期价值的问答可以归档到 `WIKI/Questions/` 或 `WIKI/Synthesis/`。

一句话：`RAW/` 是证据仓库，`WIKI/` 是经过整理的知识层，[[index]] 是地图，[[log]] 是维护记录。

## 这种方式有什么优势

相比把资料散放在文件夹里，这套方式的优势是：

1. **知识会积累**：同一个问题不用每次重新从十几篇原文里拼答案，重要结论会被维护到概念页和综合页里。
2. **来源可追溯**：wiki 页面里的判断尽量链接到 `RAW/` 或 `WIKI/Sources/`，需要时可以回到原文核验。
3. **适合长期研究**：当资料越来越多时，概念页会逐渐成熟，旧结论也可以被新资料修正。
4. **减少重复劳动**：新文章导入后，Codex 可以把它和已有页面合并，而不是生成一篇孤立摘要。
5. **结构透明**：所有内容都是本地 Markdown，可读、可改、可搜索、可用 Obsidian 图谱查看。
6. **不被单一工具锁定**：即使不用某个 AI 或插件，`RAW/`、`WIKI/`、[[index]]、[[log]] 仍然是普通文件。
7. **适合人机协作**：你负责选题、判断价值和提出问题；Codex 负责整理、链接、归档和检查一致性。

## 和普通外部知识库有什么区别

这里的“普通外部知识库”主要指文件上传式知识库、网盘文档库、NotebookLM/ChatGPT 文件库、向量数据库或常见 RAG 系统。它们通常是：保存资料 → 查询时检索片段 → 临时生成答案。

这套 LLM Wiki 的核心区别是：

| 对比点 | 普通外部知识库/RAG | 这套 Obsidian LLM Wiki |
|---|---|---|
| 知识形态 | 原文或切片为主 | 原文 + 来源摘要 + 概念页 + 综合页 |
| 查询方式 | 每次查询时临时检索和拼接 | 先读被维护过的 [[index]] 和 `WIKI/`，必要时回原文 |
| 是否积累 | 答案多停留在聊天记录里 | 好答案和新结论会沉淀成 Markdown 页面 |
| 可解释性 | 依赖系统检索结果，结构不一定可见 | 页面、链接、来源和日志都可直接查看 |
| 修改方式 | 常常需要重新上传、重建索引或依赖平台 | 直接编辑本地 Markdown，并保留维护记录 |
| 长期演化 | 容易变成资料堆 | 可以不断修订概念、合并重复、标记冲突 |
| 调用成本 | 查询时反复从原文推导 | 常用结论已编译到 wiki，后续调用更省上下文 |
| 私有与迁移 | 取决于平台 | 本地文件优先，容易备份、迁移、Git 管理 |

## 使用或调用上的区别

普通知识库更像“搜索引擎”：你问问题，它从资料里找片段，再临时组织答案。

这套 wiki 更像“研究助理维护的资料库”：

1. **导入时就处理**：新资料进来时，不只是存档，还会提取观点、实体、概念、证据和问题。
2. **查询时先看知识层**：Codex 会先看 [[index]] 和相关 `WIKI/` 页面，而不是直接在所有原文里盲搜。
3. **答案可以反哺知识库**：如果一次问答产生了有价值的结论，可以继续归档成 `WIKI/Questions/` 或 `WIKI/Synthesis/`。
4. **同一主题会越来越好用**：问得越多、导入越多，相关页面越成熟，后续回答越稳定。
5. **可以维护矛盾和不确定性**：不是强行给一个答案，而是把“需要核验”“资料之间冲突”“证据不足”写进页面。

所以，它不适合只想临时问一次的问题；它更适合长期积累一个主题，比如英语学习、研究课题、个人成长、课程笔记、投资复盘或项目知识库。

## 新文章会自动更新 wiki 吗？

默认不会。这个 vault 目前不是后台自动监听系统。你把文章放进 `RAW/` 或 `INBOX/` 后，只是完成了“保存资料”这一步；`WIKI/`、[[index]] 和 [[log]] 不会自己变化。

需要你明确叫 Codex 处理，例如：

- “处理 `RAW/文章名.md`，更新 wiki、index 和 log。”
- “把 `INBOX/` 里的资料按主题导入。”
- “检查 `RAW/` 里哪些资料还没有对应的 source summary。”

Codex/Claudian 收到指令后，才会读取资料、创建或更新 `WIKI/Sources/` 摘要、修订相关概念页/实体页/工作流页、补双向链接，并更新 [[index]] 和 [[log]]。

如果你想接近“自动更新”，可以额外配置自动化：例如用 Codex、Claude Code、macOS `launchd`、`cron` 或其他定时任务，每天/每周自动运行类似下面的维护指令：

```text
检查 RAW/ 和 INBOX/ 是否有新资料；对未处理资料执行导入工作流，更新 wiki、index 和 log；有疑问的放到 _system/checklists/ 等待我判断。
```

这种自动化不是 Obsidian 自带能力，需要单独配置运行环境，并且要确保：

- 自动化工具有权限访问这个 vault。
- `RAW/` 原文仍然只读，不被改写。
- 每次自动维护都追加 [[log]]。
- 不确定、可能误判或需要价值判断的内容，不要强行合并，应该列入检查清单。

## 手动导入一篇文章

推荐流程：

1. 把文章保存为 Markdown，放进 `RAW/`。
2. 如果只是临时资料或还没决定是否长期保存，先放进 `INBOX/`。
3. 对 Codex 说：

```text
处理 RAW/文件名.md，更新 wiki、index 和 log。
```

4. 如果你有重点，也一起说清楚，例如：

```text
处理 RAW/文件名.md，重点关注它对现有工作流的启发，更新 wiki、index 和 log。
```

5. 处理完成后，可以从 [[index]]、[[log]] 或对应的 `WIKI/Sources/` 页面查看结果。

## 从哪里开始看

- [[WIKI/首页]]：这个 wiki 的首页。
- [[index]]：内容地图。
- [[log]]：维护历史。
